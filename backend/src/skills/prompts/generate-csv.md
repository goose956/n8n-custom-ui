# Skill: Generate CSV / Export Data

Export structured data as a downloadable CSV file.

## Rules

- Header row first, then data rows
- Properly escape fields containing commas or quotes
- Use the generate-csv tool to save the file
- Include ALL records — never truncate
